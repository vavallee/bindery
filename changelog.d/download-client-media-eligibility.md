### Added
- **Download client media eligibility and priority fallback** — download clients now feature independent "Eligible for books" and "Eligible for audiobooks" toggles (both on by default) and a configurable Priority. Grabs route only to eligible clients in priority order, automatically retrying the next eligible client if a send fails.
