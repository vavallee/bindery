### Fixed
- **Find better metadata is offered for every author** (#2406) — the author page only showed the relink action for authors with almost no metadata, so an author with a bio or photo but the wrong catalogue could only be moved to a better provider through the API. The action now appears for every author. Thanks alorentiar.
