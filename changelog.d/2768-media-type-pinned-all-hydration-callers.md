### Fixed
- **Re-bind, recommendations and explicit adds stop widening a media type you chose** ([#2768](https://github.com/vavallee/bindery/issues/2768)): the Hardcover edition hydration that runs after those actions never carried the "this format was chosen, not guessed" pin the series fill already passed, so a book you set to ebook could come back as both — with the audiobook's ASIN attached — just because the work has an audio edition somewhere on Hardcover. All four hydration callers now forward the pin, and a format that came from the provider or from the default setting is still widened as before.
