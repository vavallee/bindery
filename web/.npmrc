# Allow incremental peer-dep upgrades without hard ERESOLVE failures.
# Needed while @eslint/js@10 declares peerOptional eslint@^10 but the
# broader eslint ecosystem (react-hooks plugin) still targets ^9.
legacy-peer-deps=true
